<UserControl x:Class="Memento.UserControls.DeckEditorUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" d:Background="White"
             d:DesignHeight="1080" d:DesignWidth="1920">
    <UserControl.Resources>
        <Style TargetType="{x:Type RadioButton}" >
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type RadioButton}">
                        <BulletDecorator Background="White" Cursor="Hand">
                            <BulletDecorator.Bullet>
                                <Grid/>
                            </BulletDecorator.Bullet>

                            <TextBlock x:Name="DifficultyText" FontSize="43.2" Margin="0 0 0 12">
                                <ContentPresenter />
                            </TextBlock>
                        </BulletDecorator>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="true">
                                <Setter TargetName="DifficultyText" Property="FontWeight" Value="Bold"/>
                                <Setter TargetName="DifficultyText" Property="TextDecorations" Value="Underline"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="48"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="36"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="48"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text=" Memento - DeckEditor" Grid.ColumnSpan="2" FontSize="28.8"/>

        <Grid Grid.Row="1" Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="60"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="24"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0 48 0 0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="30"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="60"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Grid Grid.Column="1" VerticalAlignment="Center">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="288"/>
                    </Grid.ColumnDefinitions>

                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="Difficulty" FontSize="57.6" Grid.Row="0"
                                 HorizontalAlignment="Center"/>
                    <StackPanel Grid.Row="1">
                        <RadioButton Content="Beginner"
                                 HorizontalAlignment="Center"/>
                        <RadioButton Content="Intermediate"
                                 HorizontalAlignment="Center"/>
                        <RadioButton Content="Advanced"
                                 HorizontalAlignment="Center"/>
                        <RadioButton Content="None" IsChecked="True"
                                 HorizontalAlignment="Center"/>
                    </StackPanel>
                </Grid>

                <Rectangle Grid.Column="3"
                       Width="720"
                       Height="480"
                       Stroke="DarkGray"
                       Fill="LightGray"
                       HorizontalAlignment="Center"/>

                <TextBlock Grid.Column="3" Text="Image" FontSize="76.8"
                       HorizontalAlignment="Center" Foreground="Gray"
                       VerticalAlignment="Center"/>

                <Image Grid.Column="3" Source="{Binding CurrentCard.ImagePath}"
                       ImageFailed="Image_ImageFailed"/>

                <Button Grid.Column="3" Click="Button_Click">
                    <Button.Style>
                        <Style TargetType="{x:Type Button}">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type Button}">
                                        <Border Background="{TemplateBinding Background}">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>

            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="48"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Grid Grid.Column="0" VerticalAlignment="Center">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="36"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBox Grid.Row="0" Width="420" Height="84" x:Name="WordTextBox"
                             Background="#F7F7F7F7" TextAlignment="Center"
                             VerticalContentAlignment="Center" FontSize="28.8"/>

                    <TextBlock Grid.Row="0" IsHitTestVisible="False" Text="Word"
                           HorizontalAlignment="Center" Foreground="DarkGray"
                           VerticalAlignment="Center" FontSize="28.8">
                        <TextBlock.Style>
                            <Style TargetType="{x:Type TextBlock}">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Text, ElementName=WordTextBox}" Value="">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>

                    <TextBox Grid.Row="2" Width="420" Height="84" Background="#F7F7F7F7" x:Name="TranscriptionTextBox"
                             TextAlignment="Center" VerticalContentAlignment="Center" FontSize="28.8"/>

                    <TextBlock Grid.Row="2" IsHitTestVisible="False" Text="Transcription"
                           HorizontalAlignment="Center" Foreground="DarkGray"
                           VerticalAlignment="Center" FontSize="28.8">
                        <TextBlock.Style>
                            <Style TargetType="{x:Type TextBlock}">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Text, ElementName=TranscriptionTextBox}" Value="">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </Grid>

                <TextBox Name="DescriptionTextBox" Background="#F7F7F7F7"
                         Grid.Column="2" Width="840" Height="240" FontSize="28.8"/>

                <TextBlock Grid.Column="2" IsHitTestVisible="False" Text="Description..."
                           HorizontalAlignment="Left" FontSize="28.8"
                           Margin="7.2 2.4 0 0" Foreground="DarkGray">
                    <TextBlock.Style>
                        <Style TargetType="{x:Type TextBlock}">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Text, ElementName=DescriptionTextBox}" Value="">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Grid>

            <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="Save Card" Margin="0 0 120 0" Width="240" Height="48" FontSize="28.8"/>
                <Button Content="Cancel" Width="240" Height="48" FontSize="28.8"/>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1" Grid.Column="3">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="24"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="24"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="24"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <ComboBox Grid.Row="0" Name="DecksComboBox" Height="48" Width="288"
                      ItemsSource="{Binding DeckEditor.Deck}" FontSize="28.8"/>

            <StackPanel Grid.Row="2" Orientation="Horizontal">
                <Button Content="New Card" Width="204" Height="48" Margin="0 0 12 0" FontSize="28.8"/>
                <Button Content="Remove Card" Width="204" Height="48" FontSize="28.8"/>
            </StackPanel>

            <TextBlock Grid.Row="4" Text="Cards: " FontSize="28.8"/>

            <ScrollViewer Grid.Row="6">
                
            </ScrollViewer>
        </Grid>
    </Grid>
</UserControl>
